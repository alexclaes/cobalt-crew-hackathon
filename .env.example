# Copy this file to .env and add your key there. The app reads OPENAI_API_KEY from .env only (not from this file).
# OpenAI API key (required for place enrichment: cost, rating, vegan/veg, etc.)
OPENAI_API_KEY=
